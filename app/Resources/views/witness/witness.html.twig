{% extends 'base.html.twig' %}

{% block title %} Witnesses {% endblock %}

{% block stylesheets %}
    {{ parent() }}
{% endblock %}

{% block javascripts %}
    {{ parent() }}
{% endblock %}

{% block navblock %}
    {{ parent() }}

    <form class="nav navbar-form navbar-left" action="" method="post">
        <div class="input-group">
            <input type="text" class="form-control" placeholder="Search Witness" name="search">
            <div class="input-group-btn">
                <button class="btn btn-default" type="submit">
                    <i class="glyphicon glyphicon-search"></i>
                </button>
            </div>
        </div>
    </form>
    <ul class="nav navbar-nav navbar-right">
        <li><a href={{ path('witnessAdd') }}>Add Witnesses</a></li>
    </ul>

{% endblock %}

{% block breadcrumb %}
    {{ parent() }}
{% endblock %}

{% block body %}
    {{ parent() }}

    <div>
        <main>
            <center>
                <h3>Witnesses</h3>
                <div class="container">
                    <div class="row" style="overflow: auto; white-space: nowrap">
                        <table class="table table-hover">
                            <thead>
                            <tr>
                                <th>#</th>
                                <th>Witness Name</th>
                                <th>NIC</th>
                                <th>Address</th>
                                <th>Mobile</th>
                                <th>Fixed</th>
                                <th></th>
                                <th></th>
                                <th></th>
                            </tr>
                            </thead>
                            <tbody>
                            {% for witness in witnesses %}
                                <tr>
                                    <td>{{ loop.index }}</td>
                                    <td>{{ witness.name }}</td>
                                    <td>{{ witness.nic }}</td>
                                    <td>{{ witness.address }}</td>
                                    <td>{{ witness.mobile }}</td>
                                    <td>{{ witness.fixed }}</td>
                                    <td><a class="btn btn-default btn-sm"
                                           href={{ path('witnessView', {'id': witness.id}) }}>View</a></td>
                                    <td><a class="btn btn-default btn-sm"
                                           href={{ path('witnessEdit', {'id': witness.id}) }}>Edit</a></td>
                                    <td>
                                        <form action="{{ path('loan') }}" method="post">
                                            <button type="submit" class="btn btn-default btn-sm" name="witnessId"
                                                    value="{{ witness.id }}">Loans
                                            </button>
                                        </form>
                                    </td>
                                </tr>
                            {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>
                <div class="navigation">
                    {{ knp_pagination_render(witnesses) }}
                </div>
            </center>
        </main>
    </div>

    <script>

    </script>

{% endblock %}